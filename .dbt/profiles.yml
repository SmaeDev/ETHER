eth:
  outputs:
    ci:
      account: CJEHXNW-EA87300
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: accountadmin
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: dbt
      warehouse: compute_wh    
  target: dev

